PORT=5000
JWT_SECRET=silada_hotel_super_secret_key_2026_luxury_booking_system
NODE_ENV=development

# AI Provider Configuration ('auto' | 'groq' | 'gemini')
AI_PROVIDER=auto

# Groq API Configuration
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b

# Google Gemini API Configuration (Alternative / Fallback)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Optional: Set a real MongoDB URI for production (otherwise uses Memory Server)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/silada-hotel
